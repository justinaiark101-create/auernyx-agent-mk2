# Mk2 Branches — Codex Rules (Overrides)

Branches may add rules, but may not weaken trunk rules.

- Must accept canonical envelopes.
- Must verify canonical bytes + digest.
- Must refuse unsafe/replayed input.
- Writes only when explicitly armed and env-gated.
- Must pass trunk proof battery unchanged.

